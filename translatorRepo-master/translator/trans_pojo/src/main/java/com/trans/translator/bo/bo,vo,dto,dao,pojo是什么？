bo：（简单理解和使用）前端把需要传递给后端的参数使用bo封装，进行发送。
    【概念理解】BO（Business Object）业务层对象，是简单的真实世界的软件抽象，通常位于中间层。
  BO 的主要作用是把业务逻辑封装为一个对象，这个对象可以包括一个或多个其它的对象。

vo：（简单理解和使用）后端把前端需要的数据使用vo封装，进行发送。
    【概念理解】VO（view object）是抽象出的业务对象，可以和表对应，也可以不，这根据业务的需要。
  对于页面上要展示的对象，可以封装一个VO对象，将所需数据封装进去。

po：（简单理解和使用）和数据库表名/字段名 一一对应的对象
    【概念理解】persistent object，持久对象。与数据库里表字段一一对应。
    PO是一些属性，以及set和get方法组成。一般情况下，一个表，对应一个PO。是直接与操作数据库的crud相关。

dto：(data transfer object)数据传输对象。主要用于远程调用等需要大量传输对象的地方

pojo：(plain ordinary java object) 简单无规则 java 对象 ,纯的传统意义的 java 对象

dao：是三层中的一层，也叫mapper层。是后端和数据库交互的层级。

